/**
 * In the context of pitch detection, particularly in signal processing and audio analysis, «τ» (tau) is often used to 
 * denote a time lag or delay. This is a key concept in methods like the autocorrelation function (ACF) and the average 
 * magnitude difference function (AMDF), which are techniques used to estimate the fundamental frequency of a periodic signal, 
 * such as musical notes or speech.
 * 
 * @type {Tau}
 */
export type Tau = number;
